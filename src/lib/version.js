// Versión de la app, en un solo lugar.
//
// SUBIRLA EN CADA PUBLICACIÓN, junto con CACHE_NAME en sw.js. Sin build no hay forma de
// generarla sola, y es lo único que permite saber —desde el teléfono, sin consola— si lo
// que se está viendo es lo último o una versión vieja que quedó cacheada.
export const APP_VERSION = 'v6 · 20 ago 2026';
